import { Resend } from 'resend';

const resend = new Resend(process.env.RESEND_API_KEY);

// TODO: render method
// Sending email method
// Call resend "send"
// Install react-email CLI for running the email tester

export default resend;
